# AIOS Bible — Platform
## 000 — Lifecycle Management Service (LMS)

| Property | Value |
|----------|-------|
| Status | Active |
| Version | 1.0 |
| Category | Bible — Platform |
| Document ID | AIOS-BBL-005-LMS-000 |
| Source Laws | Law 6 — Law of Lifecycle Compliance |
| Source Physics | Physics/006-Lifecycles.md |
| Supersedes | Nothing |
| Superseded By | Nothing |
| Amended By | RFC |

## Purpose

The Lifecycle Management Service manages the operational lifecycle of every constitutional entity. It is the state machine engine responsible for validating, authorizing, and executing lifecycle transitions, maintaining the authoritative current state for every entity, and producing immutable Events for every transition. LMS is the constitutional authority for lifecycle management — no entity bypasses LMS for state transitions.

## Canonical 10-State Lifecycle

```
Created → Planned → Assigned → Running → Waiting → Paused → Blocked → Review → Completed → Archived
```

| State | Description | Can Act? | Terminal? |
|-------|-------------|----------|-----------|
| **Created** | Entity record exists, identity assigned | No | No |
| **Planned** | Purpose, scope, and resources are defined | No | No |
| **Assigned** | Resources and capabilities are allocated | No | No |
| **Running** | Entity is actively performing its function | Yes | No |
| **Waiting** | Entity waits for external input or dependency | No | No |
| **Paused** | Entity execution is suspended, resumable | No | No |
| **Blocked** | Entity cannot proceed due to unresolved issue | No | No |
| **Review** | Entity requires review before completion | Limited | No |
| **Completed** | Entity has finished its function successfully | No | No |
| **Archived** | Entity record preserved for audit, not operational | No | Yes |

## LMS Operations

```
createEntity(type, metadata) → entity_id
transitionEntity(entity_id, target_state, authorized_by) → Event
getState(entity_id) → current_state
getLifecycleHistory(entity_id) → Event[]
validateTransition(entity_id, target_state) → ValidationResult
getEntitiesByState(state) → entity_id[]
```

### Transition Authorization

Each transition requires a specific authorizing entity type per the transition auth matrix (Foundations/008-Object-Lifecycle.md). LMS validates that the requesting entity has constitutional authority to trigger the transition before execution.

### State-Dependent Capabilities

LMS enforces that entity capabilities are bound by current lifecycle state. An entity in Running state has full capability scope. An entity in Paused state has no execution capabilities. LMS rejects any action that requires capabilities not available in the entity's current state.

### Event Sourcing

LMS is event-sourced. Every transition produces an Event written to the Event Store. The current state of any entity is a projection of its event log. LMS reconstructs state by replaying Events in order. Snapshotting compresses long event streams for performance.

### Clustering

LMS operates in active-passive configuration with read replicas. The active instance handles all write operations (transitions). Read replicas serve state queries and lifecycle history. Failover is automatic within 5 seconds. Write operations are paused during failover and resumed once the standby becomes active.

## LMS Events

| Event Type | Produced When | Fields |
|-----------|--------------|--------|
| `LMS.EntityCreated` | A new entity is registered | entity_id, entity_type, created_by, initial_state, timestamp |
| `LMS.StateChanged` | An entity transitions to a new state | entity_id, from_state, to_state, authorized_by, rationale, timestamp |
| `LMS.TransitionDenied` | A transition request is rejected | entity_id, from_state, attempted_to, reason, requested_by, timestamp |
| `LMS.EntityCompleted` | An entity reaches the Completed state | entity_id, completed_by, completion_evidence, timestamp |
| `LMS.EntityArchived` | An entity is archived | entity_id, retention_period, archived_at, timestamp |

## Cross-Cutting Concerns

### Security

Every transition requires authentication of the requesting entity and authorization by the Security Council. LMS validates the authorization token before executing any transition. Failed authorization attempts produce security Events. LMS itself authenticates to the Event Store for write operations.

### Evidence

Every LMS operation produces at least one Event. Transition Events include from-state, to-state, authorizing entity, rationale, and timestamp. The complete lifecycle history of every entity is queryable from the Event Store.

### Lifecycle

LMS manages its own lifecycle — it is a constitutional entity with lifecycle states: Created → Active → Suspended → Recovering → Terminated. LMS health is monitored by IRS. LMS failover is managed by the Platform availability system.

### Capability Bounds

LMS can only perform lifecycle operations. It has no authority to modify entity data, allocate resources, or execute domain logic. LMS capabilities are: read state, write transitions, validate transitions, query history. LMS cannot create or destroy entities outside its lifecycle scope.

### Communication

LMS communicates exclusively through ACF. All transition requests arrive via ACF messages. All Events are published to ACF streams. LMS does not expose HTTP or gRPC endpoints — all interaction is through ACF message passing.

### Design DNA

| Rule | Assessment |
|------|-----------|
| R1 — Modulsingularity | LMS does one thing: lifecycle management |
| R2 — Dependency Order | LMS depends on Event Store and ACF; nothing depends on LMS internals |
| R3 — DRY | Lifecycle definitions are stored once in the Lifecycle Registry |
| R4 — Builder Pattern | Entities are built by their respective factories, not by LMS |
| R5 — Liskov | All entity lifecycles implement the same Lifecycle interface |
| R6 — DI over Singletons | LMS receives ACF and Event Store as injected dependencies |
| R7 — Tests Exist | Every transition path has unit and integration tests |
| R8 — Tests Fast | LMS transition tests complete in <100ms |
| R9 — Deterministic | Same transition inputs always produce same state result |
| R10 — Simpler Over Complex | LMS state machine is a linear DAG, no branching state logic |
| R11 — Refactor Over Rewrite | LMS lifecycle definitions evolve via versioned updates |
| R12 — Embrace Errors | Every transition failure has a unique error code |
| R13 — Design for Failure | LMS active-passive failover; Event replay restores state |
| R14 — Paved Path | LMS is the only path for lifecycle transitions |
| R15 — Open/Closed | New entity lifecycles extend without modifying LMS core |

## Related Documents

| Document | Relationship |
|---------|-------------|
| Physics/006-Lifecycles.md | Lifecycle invariants — source authority |
| Foundations/008-Object-Lifecycle.md | Transition auth matrix, canonical states |
| 001-State-Machine.md | State machine engine used by LMS |
| 002-Transition-Validator.md | Transition validation rules enforced by LMS |
| 004-EVS.md | Event Store — LMS persists Events here |
| Physics/005-Events.md | Event invariants — every transition produces an Event |
| Physics/004-Sessions.md | Session lifecycle — managed by LMS |
| Physics/002-Missions.md | Mission lifecycle — managed by LMS |
| Physics/003-Organizations.md | Organization lifecycle — managed by LMS |
| Core/OSYS/002-Org-Lifecycle.md | Organization-specific lifecycle implementation |
